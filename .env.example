# NJ Ski Haus API — Environment Variables
# Copy this to .env.local for local development
# Set these in Vercel dashboard → Settings → Environment Variables for production

# ── Vercel KV (auto-populated when you link a KV database in Vercel dashboard) ──
# These are set automatically by Vercel when you add a KV store — don't set manually
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# ── Cron job protection secret ──
# Generate any random string: openssl rand -hex 32
# Set this in Vercel → Settings → Environment Variables
# Then use it to call /api/scrape manually: GET /api/scrape?secret=YOUR_SECRET
CRON_SECRET=your-random-secret-here

# ── Optional: your main site domain (for CORS) ──
ALLOWED_ORIGIN=https://njskihaus.com
